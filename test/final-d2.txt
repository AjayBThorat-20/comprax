======================================================================
PROJECT: comprax
======================================================================
Runtime: Node.js
Total Files: 5
======================================================================

## src/utils/logger.js
EXPORTS: startSpinner, succeedSpinner, updateSpinner, failSpinner, logInfo, logSuccess, logWarning, logError

SUMMARY:
Exports: startSpinner, succeedSpinner, updateSpinner, failSpinner, logInfo, logSuccess, logWarning, logError

Functions:
  startSpinner(text)
  succeedSpinner(text)
  updateSpinner(text)
  failSpinner(text)
  logInfo(message)
  logSuccess(message)
  logWarning(message)
  logError(message)

Imports: 2 modules

## src/core/cache/hash.js
EXPORTS: hashContent, loadCache, saveCache, getCachedData, setCachedData, hasFileChanged

SUMMARY:
Exports: hashContent, loadCache, saveCache, getCachedData, setCachedData, hasFileChanged

Functions:
  hashContent(content)
  loadCache(cacheDir)
  saveCache(cacheDir, cache)
  getCachedData(filepath, cache)
  setCachedData(filepath, content, processedData, cache)
  hasFileChanged(filepath, content, cache)

Imports: 3 modules

## src/core/formatter.js
EXPORTS: formatCombined, formatCombinedHybrid, formatDirectory, formatDirectoryHybrid, formatSummary

SUMMARY:
Exports: formatCombined, formatCombinedHybrid, formatDirectory, formatDirectoryHybrid, formatSummary

Functions:
  formatCombined(fileData, basePath)
  formatCombinedHybrid(fileData, basePath, stack, projectName, semantic)
  formatDirectory(files, dirName)
  formatDirectoryHybrid(files, dirName, semantic)
  formatSummary(organized, stats, stack, projectName)

Imports: 1 module

## src/core/scanner.js
EXPORTS: resetScanner, scanDir, getTotalFiles

SUMMARY:
Exports: resetScanner, scanDir, getTotalFiles

Functions:
  resetScanner()
  scanDir(dir, depth, visited)
  getTotalFiles()

Imports: 2 modules

## src/core/organizer.js
EXPORTS: organizeByDirectory, getSafeFilename, createTreeStructure

SUMMARY:
Exports: organizeByDirectory, getSafeFilename, createTreeStructure

Functions:
  organizeByDirectory(files, projectPath)
  getSafeFilename(dirPath)
  createTreeStructure(organized)

Imports: 1 module

